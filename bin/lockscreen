#!/bin/bash
# This script is for my screen locker.
# It takes a screenshot and blurs it, which is then used for the background.

DIR="/tmp/"
FILE="lock_background.png"
scrot $DIR$FILE &&
convert -blur 0x3 $DIR$FILE $DIR$FILE &&

ln -fs $DIR$FILE $HOME"/.config/lock_background.png" &&

slimlock
